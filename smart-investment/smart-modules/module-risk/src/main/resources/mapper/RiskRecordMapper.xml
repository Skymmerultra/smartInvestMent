<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smart.investment.module.risk.mapper.RiskRecordMapper">

    <select id="selectByPortfolioId" resultType="com.smart.investment.module.risk.entity.RiskRecord">
        SELECT * FROM risk_record WHERE portfolio_id = #{portfolioId} ORDER BY created_at DESC
    </select>

    <select id="selectRecentByPortfolioId" resultType="com.smart.investment.module.risk.entity.RiskRecord">
        SELECT * FROM risk_record WHERE portfolio_id = #{portfolioId} ORDER BY created_at DESC LIMIT #{limit}
    </select>

    <select id="selectByRiskType" resultType="com.smart.investment.module.risk.entity.RiskRecord">
        SELECT * FROM risk_record WHERE risk_type = #{riskType} ORDER BY created_at DESC
    </select>

</mapper>